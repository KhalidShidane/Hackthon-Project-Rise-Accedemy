export default function Pagination({ page, totalPages, onPageChange }) {
  return <nav aria-label="Freelancer pages" className="mt-8 flex justify-center gap-2"><button disabled={page === 1} onClick={() => onPageChange(page - 1)} className="h-9 w-9 rounded-lg border border-slate-200 bg-white text-sm font-semibold text-slate-600 disabled:cursor-not-allowed disabled:opacity-40" aria-label="Previous page">&lt;</button>{Array.from({ length: totalPages }, (_, index) => <button key={index} onClick={() => onPageChange(index + 1)} className={`h-9 w-9 rounded-lg text-sm font-semibold ${page === index + 1 ? "bg-blue-700 text-white" : "border border-slate-200 bg-white text-slate-600 hover:bg-slate-50"}`}>{index + 1}</button>)}<button disabled={page === totalPages} onClick={() => onPageChange(page + 1)} className="h-9 w-9 rounded-lg border border-slate-200 bg-white text-sm font-semibold text-slate-600 disabled:cursor-not-allowed disabled:opacity-40" aria-label="Next page">&gt;</button></nav>;
}
