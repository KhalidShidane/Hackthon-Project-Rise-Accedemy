import { useEffect, useState } from "react";
import axios from "axios";
import { Search, Trash2 } from "lucide-react";
import { Link } from "react-router-dom";
import { useAuth } from "../context/auth";
const API="http://localhost:5000";
const badges={active:"bg-emerald-50 text-emerald-700",pending:"bg-amber-50 text-amber-700",suspended:"bg-orange-50 text-orange-700",blocked:"bg-red-50 text-red-700"};
export default function AdminUsers(){const {token}=useAuth();const [users,setUsers]=useState([]);const [loading,setLoading]=useState(true);const [search,setSearch]=useState("");const [role,setRole]=useState("all");const [status,setStatus]=useState("all");const [error,setError]=useState("");const headers={Authorization:`Bearer ${token}`};const load=async()=>{setLoading(true);try{const {data}=await axios.get(`${API}/api/admin/users`,{headers});setUsers(Array.isArray(data?.users)?data.users:[]);setError("")}catch(err){setUsers([]);setError(err.response?.data?.message||"Unable to load users.")}finally{setLoading(false)}};useEffect(()=>{load()},[token]);const update=async(user,next)=>{if(!window.confirm(`Change ${user.name}'s account to ${next}?`))return;try{await axios.patch(`${API}/api/admin/users/${user._id}/status`,{status:next},{headers});load()}catch{setError("Could not update account status.")}};const remove=async(user)=>{if(!window.confirm(`Delete ${user.name}? This cannot be undone.`))return;try{await axios.delete(`${API}/api/admin/users/${user._id}`,{headers});setUsers(current=>current.filter(item=>item._id!==user._id))}catch(err){setError(err.response?.data?.message||"Could not delete this user.")}};const shown=users.filter(user=>{const text=`${user.name||""} ${user.email||""}`.toLowerCase();return text.includes(search.toLowerCase())&&(role==="all"||user.role===role)&&(status==="all"||(user.status||"active")===status)});if(loading)return <div className="rounded-2xl bg-white p-10 text-center text-slate-500">Loading users…</div>;return <><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-semibold text-emerald-700">USER MANAGEMENT</p><h1 className="mt-1 text-3xl font-bold text-slate-900">All users</h1><p className="mt-2 text-slate-500">Manage Freelancer Hub Somalia accounts from one place.</p></div><div className="rounded-xl bg-emerald-50 px-5 py-3 text-sm font-bold text-emerald-800">{users.length} total users</div></div><div className="mt-7 flex flex-wrap gap-3"><div className="relative min-w-[240px] flex-1"><Search size={18} className="absolute left-3 top-3 text-slate-400"/><input value={search} onChange={event=>setSearch(event.target.value)} placeholder="Search name or email" className="w-full rounded-xl border border-slate-300 py-2.5 pl-10 pr-3 text-sm outline-none focus:border-emerald-600 focus:ring-2 focus:ring-emerald-100"/></div><select value={role} onChange={event=>setRole(event.target.value)} className="rounded-xl border border-slate-300 bg-white px-4 text-sm"><option value="all">All roles</option><option value="admin">Admin</option><option value="client">Client</option><option value="company">Company</option><option value="freelancer">Freelancer</option></select><select value={status} onChange={event=>setStatus(event.target.value)} className="rounded-xl border border-slate-300 bg-white px-4 text-sm"><option value="all">All statuses</option><option value="active">Active</option><option value="pending">Pending</option><option value="suspended">Suspended</option><option value="blocked">Blocked</option></select></div>{error&&<div className="mt-4 rounded-xl bg-red-50 p-4 text-sm text-red-700">{error} <button onClick={load} className="ml-2 font-bold underline">Try again</button></div>}<div className="mt-6 overflow-x-auto rounded-2xl border border-emerald-100 bg-white shadow-sm"><table className="min-w-[850px] w-full text-left text-sm"><thead className="bg-emerald-50/70 text-xs uppercase tracking-wide text-emerald-900"><tr><th className="p-4">User</th><th>Email</th><th>Role</th><th>Status</th><th>Registered</th><th className="p-4">Actions</th></tr></thead><tbody className="divide-y divide-slate-100">{shown.map(user=>{const current=user.status||"active";return <tr key={user._id} className="hover:bg-slate-50"><td className="p-4"><Link to={`/admin/users/${user._id}`} className="font-bold text-slate-900 hover:text-emerald-700">{user.name||"Unnamed user"}</Link></td><td className="text-slate-600">{user.email}</td><td className="capitalize">{user.role}</td><td><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${badges[current]||badges.pending}`}>{current}</span></td><td className="text-slate-500">{user.createdAt?new Date(user.createdAt).toLocaleDateString():"—"}</td><td className="p-4"><div className="flex gap-2"><select value={current} onChange={event=>update(user,event.target.value)} className="rounded-lg border border-slate-200 bg-white px-2 py-1.5 text-xs"><option value="active">Active</option><option value="pending">Pending</option><option value="suspended">Suspend</option><option value="blocked">Block</option></select>{user.role!=="admin"&&<button onClick={()=>remove(user)} aria-label={`Delete ${user.name}`} className="rounded-lg border border-red-200 px-2 text-red-600 hover:bg-red-50"><Trash2 size={16}/></button>}</div></td></tr>})}</tbody></table>{!shown.length&&<div className="p-10 text-center text-slate-500">No users match these filters.</div>}</div></>}
